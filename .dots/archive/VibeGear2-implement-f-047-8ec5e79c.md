---
title: "implement: F-047 thread per-car DamageState through raceSession + feed applyHit / applyOffRoadDamage per GDD §13"
status: closed
priority: 2
issue-type: task
created-at: "\"2026-04-26T12:46:51.289439-05:00\""
closed-at: "2026-04-26T13:04:05.317758-05:00"
close-reason: verified
---

blocks: F-047 in docs/FOLLOWUPS.md is the concrete next slice for F-019 (race-session damage integration). The damage module ships pure (src/game/damage.ts: applyHit, applyOffRoadDamage, isWrecked, PRISTINE_DAMAGE_STATE, performanceMultiplier) and the §23 NITRO_WHILE_SEVERELY_DAMAGED_BONUS slice (F-045, done) added the optional nitroActiveOnDamagedCar flag to applyHit. No in-app caller yet: raceSession.ts has zero references to applyHit, applyOffRoadDamage, DamageState, or PRISTINE_DAMAGE_STATE. This dot wires the consumer. 1. Add damage: DamageState to RaceSessionPlayerCar (src/game/raceSession.ts:309) and RaceSessionAICar (line 226), initialised to PRISTINE_DAMAGE_STATE in createRaceSession (line 475+). 2. After per-tick physics for each still-racing car, call applyOffRoadDamage(state.damage, distanceOffRoadThisTick, assistScalars) when the car is off-track. (raceSession already tracks off-road timers; reuse the same predicate.) 3. Add a vehicle-on-vehicle collision detector. raceSession does not have one yet; the simplest path is a pairwise z-distance check inside the existing per-pair scan loop (draftPairKey region) gated on |z_a - z_b| < CAR_LENGTH and same lane. On hit emit a DamageHit and call applyHit(state.damage, hit, assistScalars, nitroOnDamagedCar) where nitroOnDamagedCar = state.nitro.activeRemainingSec > 0 && (getDamageBand(state.damage.total * 100) === 'severe' || 'catastrophic'). Hazard hits feed through the same applyHit path once src/game/hazards.ts grows a damage emitter (out of scope here; document as a follow-up if needed). 4. Flip RaceCarStatus to 'dnf' with reason 'wrecked' when isWrecked(damage) crosses true. 5. assistScalars come from resolveDifficultyPreset(config.player.difficultyPreset) per F-042 (done) and are resolved once per tick like cpuModifiers. Affected files: src/game/raceSession.ts (state shape, createRaceSession, stepRaceSession), src/game/__tests__/raceSession.test.ts (collision -> damage growth, off-road -> damage growth, wrecked -> dnf, pristine when no contact). docs/FOLLOWUPS.md: F-047 marked done; F-019 marked done if the integration covers the §13 expectations the parent followup names. Verify: npm run lint / typecheck / test / build clean. Race finish without contact leaves damage at PRISTINE. A car driven off-road accumulates body damage. A wrecked car flips to dnf with reason wrecked. nitroOnDamagedCar correctly triggers the +15% NITRO_WHILE_SEVERELY_DAMAGED_BONUS when a severely-damaged car is hit while nitro is active. No em or en dashes in changed files.
