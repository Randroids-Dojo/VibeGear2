---
title: "implement: convert tireSqueal/brakeScrub from one-shot edge tones to gated continuous loops that modulate with steer load and speed"
status: done
priority: 2
issue-type: task
created-at: "2026-05-06T00:11:58.214561-05:00"
---

AUDIO CORNERING-CUE FIX. src/game/raceSession.ts:2007-2020 emits a single RaceSessionTireSquealAudioEvent at the moment the gate transitions false->true, and src/audio/sfx.ts:231 plays a 0.16 s one-shot tone for that event. Result: a sustained hairpin produces a single 160 ms chirp at the entry, then silence for the rest of the corner. §18 "Required sounds: tire squeal" + §18 "brake scrub" implies a continuous bed under cornering load, not a single chirp. Affected files: src/audio/sfx.ts (add startTireSquealLoop / stopTireSquealLoop / updateTireSquealLoop + same trio for brakeScrub via a small filtered-noise gain stage on top of the existing oscillator pattern; mirror the engineRuntime smoothing model); src/game/raceSession.ts (replace one-shot edge events with `tireSqueal: { active: bool, intensity: number }` and `brakeScrub: { active: bool, intensity: number }` per-tick state on the audio event stream, with intensity = lerp from 0 at TIRE_SQUEAL_STEER_THRESHOLD=0.65 to 1 at |steer|=1, scaled by speedFactor); src/app/race/page.tsx (route the new continuous events into the loop runtime). Implementation Notes: keep the existing one-shot ImpactSfxKind=carHit and wallHit untouched; the loop is only for the two surface-friction cues. The intensity scalar drives both gain (0.4 to 0.85) and pitch (860 to 1420 Hz at speedFactor=1.0). When the loop deactivates, ramp gain to 0 over 80 ms and stop the oscillator. Verify: Vitest src/audio/__tests__/tireSquealLoop.test.ts that tracks an active loop across 60 ticks of varied steer input and asserts the gain param trace is monotone increasing while steer climbs and ramps back to 0 within 80 ms after deactivation; Playwright e2e/tire-squeal-loop.spec.ts that holds full steer at top speed for 1 s, captures audio context node count >= 1 oscillator with non-zero gain throughout, and confirms the loop stops within 100 ms of input release. Q-018 if §18 does not pin the slip-angle / steer-magnitude onset; recommend default 0.65 steer threshold (already in raceSession.ts) and 0.18 m/s lateral-rate threshold for entering load.
